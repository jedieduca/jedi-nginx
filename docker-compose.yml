version: '3.8'

services:
  nginx_gateway:
    image: nginx:alpine
    container_name: nginx_gateway
    restart: unless-stopped
    ports:
      - "80:80"   # Mantemos a 80 para capturar e redirecionar
      - "443:443" # Nova porta para HTTPS
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ../jedi-adianti:/var/www/html # 🔹 Volume normal aqui também
      - ./certs:/etc/nginx/certs:ro # Injeta os certificados (Apenas Leitura)
      - graficos_data:/var/www/html/mad/app/output/charts:ro
    networks:
      - my-shared-network

volumes:
  graficos_data:
    external:
      name: python_service_graficos_data

networks:
  my-shared-network:
    external: true